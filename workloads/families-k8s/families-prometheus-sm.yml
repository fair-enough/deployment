apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: families-app
  namespace: default
  labels:
    team: default
spec:
  selector:
    matchLabels:
      app: families
  endpoints:
    - port: web
      path: /prometheusMetrics
